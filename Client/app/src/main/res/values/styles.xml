<resources>
    <!-- Standard activity theme -->
    <style name="ActivityTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor">@color/colorB_end</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="colorAccent">@color/colorA_start</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
